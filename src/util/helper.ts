export function positiveDifference(a: number, b: number): number {
  return Math.abs(a - b);
}

export const wentWrong = `Something went wrong! Please try again`;
export type ICategories = "mushroom" | "edible" | "microdose" | "merch";
